using System;

namespace Stamp.Domain.Entities
{
    // Represents a simple stamp-based reward rule for a business
    public class RewardRule : BaseEntity
    {
        //#region Properties
        //// Business this reward rule belongs to
        //public Guid BusinessId { get; set; }
        //// Number of stamps required to earn the reward
        //public int RequiredStamps { get; set; }
        //// Description of the reward (e.g., "50% off", "Free car wash")
        //public string RewardDescription { get; set; } = string.Empty;
        //// Date when the rule becomes active
        //public DateTime StartDate { get; set; }
        //// Date when the rule expires (nullable for no expiry)
        //public DateTime? EndDate { get; set; }
        //// Indicates if the rule is active
        //public bool IsActive { get; set; } = true;
        //#endregion
    }
}
